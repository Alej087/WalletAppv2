server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Alej087/examen-esp-backend-gonzalez-david-config
          default-label: main
          force-pull: true
          search-paths: configData
